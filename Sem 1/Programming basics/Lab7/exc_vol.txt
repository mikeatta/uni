func1(tab_letters, tab_numbers)
{
    for(arr.len)
    {
        switch
            case letter_a
            int a += 1

            case letter_b
            int b += 1

            case letter_...
    }

    for(arr.len)
    {
        switch
            case 1
            int 1 += 1

            case 2
            int 2 += 1

            case ...
    }
    return tab_letters, tab_numbers
}

func2(tab_letters, tab_numbers)
{
    printf("What symbol to check)
    if(symbol table found)
        printf(answer)
}
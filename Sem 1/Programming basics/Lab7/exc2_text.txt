getchar - single character, input from user
compare user input (getchar) to arr[i]

if (getchar == arr[i])
    return 1
else
    return 0

--------------------------------
Line 38 gets input twice. (getchar() executes twice causing the for loop to miscount)